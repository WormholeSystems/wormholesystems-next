services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: vector
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vector}
      POSTGRES_DB: vector
    # Bound to the loopback: the other containers reach it over the compose network, and
    # nothing outside the machine has any business connecting.
    ports:
      - "127.0.0.1:${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vector"]
      interval: 5s
      timeout: 5s
      retries: 10

  # The full stack (Caddy + API + frontend). `docker compose up db` still works alone for
  # local dev, where the API and frontend run on the host instead.
  api:
    build: .
    profiles: ["full"]
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgres://vector:${POSTGRES_PASSWORD:-vector}@db:5432/vector
      LISTEN_ADDR: 0.0.0.0:3000
    depends_on:
      db:
        condition: service_healthy
    volumes:
      # The unpacked SDE, ~550MB downloaded on first boot. In a volume so recreating the
      # container (every update) does not download it all over again.
      - sde:/app/data/sde

  web:
    build: frontend
    profiles: ["full"]
    restart: unless-stopped
    environment:
      API_BASE: http://api:3000
      PORT: 3001
    depends_on:
      - api

  caddy:
    image: caddy:2
    profiles: ["full"]
    restart: unless-stopped
    environment:
      # Empty means no domain, which Caddy reads as plain http on :80.
      WS_DOMAIN: ${WS_DOMAIN:-:80}
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
      - "${HTTPS_PORT:-443}:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      # Certificates live here. Losing this volume means asking Let's Encrypt for new ones,
      # which is rate limited, so it outlives the container on purpose.
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api
      - web

volumes:
  pgdata:
  sde:
  caddy_data:
  caddy_config:
