# rustfmt configuration — https://rust-lang.github.io/rustfmt/
# Stable options only, so `cargo fmt` works on the stable toolchain.
max_width = 100
newline_style = "Unix"
use_small_heuristics = "Default"
reorder_imports = true
reorder_modules = true
